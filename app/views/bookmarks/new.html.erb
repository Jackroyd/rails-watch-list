<h1>Create bookmark</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "add film to list", class: "btn btn-primary" %>
<% end %>
